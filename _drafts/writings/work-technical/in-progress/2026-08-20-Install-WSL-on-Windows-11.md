open powershell from the windows application launcher

install ubuntu

	wsl --installl

wait until the installation is completed.

check if ubuntu is installed properly by using following command

	lsb_release -a